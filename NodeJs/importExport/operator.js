const add = (a, b) => {
  return a + b;
};

const sub = (a, b) => {
  return a - b;
};
const mul = (a, b) => {
  return a * b;
};

// module.exports.add = add;
// module.exports.sub = sub;

module.exports = { add, sub, mul };



// tips for logic building
// step 1 :-  dataflow (keep continue to dry run )
//step 2:- 90-10 teaching || 

//todos app (more complex what you want);
//{
// road map for backend
//A programming language  and a database
// 
// }